await
async def approve
approve
pending
leads = pending_tasks
def ask model
def ask_model
fineleads
findleads
file
try:
for place in raw place
new_leads
async def findleads
csv
system_prompt
leads.append(lead)
del pending
app.add_handler(CommandHandler("leads", leads))
if place_id in seen_leads:    continue
leads =
async def leads
app.run
time=time
prompt = "apartment complexes phoenix az"raw_places = search_places(prompt)
"maxResultCount"
lead
lead = 
SEEN_FILE = "seen_leads_json"
search
echo "[]" > seen_leads.json
if lead_key in seen_leads:    continue
if lead
for prompt in prompts:    raw_places = search_places(prompt)
OPENAI
send_email
app.job



